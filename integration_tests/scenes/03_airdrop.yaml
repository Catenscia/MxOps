allowed_networks:
  - LOCAL
  - TEST
  - DEV

allowed_scenario:
  - "integration_test*"

steps:
  - type: ContractCall
    sender: owner
    contract_id: "abc-esdt-minter"
    endpoint: addAirdropAmount
    gas_limit: 5000000
    arguments:
      - "[user]"
      - 100000
    check_for_errors: True

  - type: ContractCall
    sender: user
    contract_id: "abc-esdt-minter"
    endpoint: claimAirdrop
    gas_limit: 5000000
    check_for_errors: True